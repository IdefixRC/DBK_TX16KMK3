## Fixes

- Fixed the low battery alert so it respects the value stored in `BatAlertPct` in the widget options.
- Removed the runtime overwrite of the alert percentage with `battery_alert_pct` from the JSON.
- The JSON now only supplies the initial default for the widget option.
