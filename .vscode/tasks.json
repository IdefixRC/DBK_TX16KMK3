{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Deploy to radio folder",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-File",
        "${workspaceFolder}\\.vscode\\deploy-radio.ps1",
        "-Source",
        "${workspaceFolder}",
        "-RadioRoot",
        "${input:radioRoot}"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "clear": true
      }
    }
  ],
  "inputs": [
    {
      "id": "radioRoot",
      "type": "promptString",
      "description": "Root folder of the radio SD card",
      "default": "E:\\"
    }
  ]
}
